# infra/docker-compose.yml
name: predmarket-poc

services:
  # Durable, реплеябельный лог сделок и сеттлмента = защита от ghost-fills.
  redpanda:
    image: redpandadata/redpanda:v24.2.7
    command:
      - redpanda
      - start
      - --smp=1
      - --overprovisioned
      - --node-id=0
      - --kafka-addr=PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr=PLAINTEXT://localhost:9092
    ports:
      - "9092:9092"
      - "9644:9644"
    volumes:
      - redpandadata:/var/lib/redpanda/data
    healthcheck:
      test: ["CMD-SHELL", "rpk cluster health | grep -q 'Healthy:.*true'"]
      interval: 5s
      timeout: 5s
      retries: 15

  # Понадобятся позже — раскомментируем по мере надобности:
  #
  # postgres:
  #   image: timescale/timescaledb:2.17.2-pg16
  #   environment:
  #     POSTGRES_USER: predmarket
  #     POSTGRES_PASSWORD: predmarket
  #     POSTGRES_DB: predmarket
  #   ports: ["5432:5432"]
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #
  # redis:
  #   image: redis:7-alpine
  #   command: ["redis-server", "--appendonly", "yes"]
  #   ports: ["6379:6379"]
  #   volumes:
  #     - redisdata:/data
  #
  # anvil:
  #   image: ghcr.io/foundry-rs/foundry:latest
  #   entrypoint: ["anvil"]
  #   command: ["--host", "0.0.0.0", "--chain-id", "31337", "--block-time", "1"]
  #   ports: ["8545:8545"]

volumes:
  redpandadata:
  # pgdata:
  # redisdata: